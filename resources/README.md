# Pasta de Recursos

Esta pasta contém apenas os binários necessários para o conversor funcionar.

1. **Binários**
   Coloque os executáveis `ffmpeg` e `ffprobe` aqui.
   Os scripts `setup.sh` e `setup.bat` já fazem essa cópia automaticamente.
